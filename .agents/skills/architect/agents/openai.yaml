interface:
  display_name: "Architect"
  short_description: "Plan/review in main; delegate all delivery"
  default_prompt: "Use $architect: keep the main thread as planner and reviewer, and delegate all implementation, fixes, and automated tests to bounded subagents."
