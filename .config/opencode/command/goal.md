---
description: Set or update the persistent project goal.
agent: architect
---

Use `$ARGUMENTS` as the requested persistent goal for this project.

If it is empty, vague, or unclear, ask clarifying questions before editing anything.

Otherwise create or update `.opencode/goal.md` in the current project and structure it with:
- create `.opencode/` if needed before writing the file
- a title
- the goal
- acceptance criteria
- current status / next steps
- last updated date, if available

Do not edit unrelated files unless they are needed to accomplish the goal and the user expects that work.
After saving the goal file, continue working toward the goal as appropriate.
